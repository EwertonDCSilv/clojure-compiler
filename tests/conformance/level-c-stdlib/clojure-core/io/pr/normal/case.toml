id = "c.clojure_core.pr.normal"
level = "C"
area = "stdlib/clojure-core/io"
status = "xfail"
class = "expected-diff"
target = "build-run"
oracle = "expected-diff"
timeout_ms = 10000
gc_stress = false
reason = "Deliberate native/JVM difference: native *out* routing of pr; the JVM oracle records the expected divergence (ADR-0016)."
tracking = "specs/IO_SPEC.md"
namespace = "clojure.core"
