id = "c.clojure_core.count_if"
level = "C"
area = "stdlib/clojure-core"
status = "active"
class = "official"
target = "build-run"
oracle = "not-applicable"
timeout_ms = 10000
gc_stress = false
reason = "JVM oracle cannot evaluate the native count-if function; verified natively and excluded from the JVM oracle (ADR-0016)."
tracking = "crates/clojure-native-cli/src/core_compiled.clj"
namespace = "clojure.core"
