id = "c.cljn_io.filesystem.isolated_tree_and_symlink"
level = "C"
area = "stdlib/cljn-io/filesystem"
status = "active"
class = "official"
target = "build-run"
oracle = "not-applicable"
timeout_ms = 10000
gc_stress = true
reason = "Native cljn.io recursive/attribute operations over the compiled module and runtime primitives; the Clojure/JVM oracle cannot resolve the native namespace (ADR-0016, issue #144)."
tracking = "specs/IO_SPEC.md#filesystem-and-recursive-safety"
namespace = "cljn.io"
[run]
platforms = ["linux"]
setup_symlinks = [{ path = "source/link", target = "a.txt" }]
expected_symlinks = [{ path = "copy/link", target = "a.txt" }]
