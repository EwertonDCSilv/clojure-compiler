#!/usr/bin/env bash
# Run fast, staged-file-aware repository checks before creating a commit.
set -euo pipefail

repository="$(git rev-parse --show-toplevel)"
"${repository}/scripts/check-agent-story-points.sh"
exec "${repository}/scripts/pre-commit.sh"
