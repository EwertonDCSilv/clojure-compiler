#!/usr/bin/env bash
# Run the complete local quality gate, including runtime sanitizers, before push.
set -euo pipefail

repository="$(git rev-parse --show-toplevel)"
"${repository}/scripts/check-agent-story-points.sh"
exec make -C "${repository}" pre-push
